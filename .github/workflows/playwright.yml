name: Playwright Tests (Docker)

on:
    push:
        branches: [ main ]
    pull_request:
        branches: [ main ]

jobs:
    playwright:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout repository
              uses: actions/checkout@v4

            - name: Build Docker image
              run: docker build -t playwright-tests .

            - name: Run tests inside container
              run: |
                docker run --name playwright-container playwright-tests
                
            - name: Copy Allure results from container
              run: |
                docker cp playwright-container:/app/allure-results ./allure-results
                docker cp playwright-container:/app/allure-report ./allure-report

            - name: upload Allure Report
              uses: actions/upload-artifact@v4
              with:
                  name: allure-report
                  path: allure-report
